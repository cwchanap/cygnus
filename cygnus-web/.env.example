# Optional: TensorFlow.js model URL for in-browser drum transcription.
# Leave unset to use /models/drums/model.json by default and fall back to the
# browser-side signal-processing path if the model is unavailable.
# Example (place model.json and weight shards under public/models/drums/):
# PUBLIC_TFJS_MODEL_URL=/models/drums/model.json
